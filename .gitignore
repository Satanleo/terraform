keys
# Local .terraform directories
**/.terraform/*

# .tfstate files (contain sensitive data and infrastructure state)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (may contain sensitive data like passwords, keys)
*.tfvars
*.tfvars.json

# Ignore override files (local development)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore lock files (optional - some teams commit this)
# .terraform.lock.hcl